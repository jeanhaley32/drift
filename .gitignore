__pycache__/
*.pyc
.DS_Store

# built Wi-Fi helper app (rebuild locally via wifi-helper/setup.sh)
wifi-helper/DriftWiFi.app/

# local board manifest — may name private repos; copy driftboards.example.json
driftboards.json

# ROUTES board hardcodes internal infra specifics (resource-name prefix, service
# names, ALB topology) — keep local until genericized.
driftboards/routes.py
